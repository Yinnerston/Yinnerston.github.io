name: deploy-on-push

on: 
  push:
    branches:
      - main

jobs:
  deploy:

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: my-app

    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js 16.x
        uses: actions/setup-node@v3
        with:
          node-version: 16.x
      - name: Install project
        run: npm install
      - run: git config --global user.name "Yinnerston"
      - run: git config --global user.email "nathanzjyin@gmail.com"
      - name: Deploy project
        run: npm run deploy